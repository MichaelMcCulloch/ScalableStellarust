---
- name: Copy docker config files to hosts
  copy:
    src: "{{item.file}}"
    dest: "{{item.path}}"
    backup: no
  with_items:
    - file: daemon.json
      path: /etc/docker/
    - file: taskmax.conf
      path: /etc/systemd/system/docker.service.d/
  tags:
    - config
    - docker
    - file
