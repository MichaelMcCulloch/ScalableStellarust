---
- name: Copy and apply ingress.yml
  block:
    - name: Copy ingress.yml
      template:
        src: ingress.yml.j2
        dest: $HOME/ingress.yml
        backup: no

    - name: create ingress.yml
      shell: kubectl apply -f $HOME/ingress.yml
