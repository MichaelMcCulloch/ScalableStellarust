---
- name: Create htpasswd-dashboard document
  shell: "htpasswd -bnm {{htpasswd_user}} {{htpasswd_pass}} > auth"

- name: Convert htpasswd To a Secret
  shell: kubectl -n kubernetes-dashboard create secret generic htpasswd-dashboard --from-file=auth

- name: remove auth file
  file:
    path: auth
    state: absent
